const initialState = {
    active: "/horarios",
}

const horario = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default horario