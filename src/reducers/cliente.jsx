const initialState = {
    active: "/clientes",
}

const cliente = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default cliente