const initialState = {
    active: "/usuarios",
}

const usuario = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default usuario