const initialState = {
    active: "/compras",
}

const compras = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default compras