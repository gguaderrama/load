const initialState = {
    active: "/productos",
}

const producto = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default producto